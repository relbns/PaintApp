{
  "name": "paint-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:client": "npm install --prefix client",
    "install:server": "npm install --prefix server",
    "install": "npm run install:client && npm run install:server",
    "dev:client": "npm run serve --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev": "npm-run-all --parallel dev:client dev:server",
    "dev:local": "cd client && VUE_APP_USE_SERVER=false npm run serve",
    "dev:server-mode": "cd client && VUE_APP_USE_SERVER=true npm run serve",
    "start": "npm start --prefix server",
    "build": "npm run build --prefix client",
    "build:local": "cd client && VUE_APP_USE_SERVER=false npm run build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
